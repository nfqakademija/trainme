{% extends 'base.html.twig' %}

{% block title %}Customer registration{% endblock %}

{% block body %}
    <div class="heroLogin"></div>
    <div class="loginContainer">
        <h1 class="blackTitle u-mgBtm">Customer registration</h1>

        {% form_theme form 'form/template.html.twig' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
